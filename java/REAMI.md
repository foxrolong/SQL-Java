bài tập:

```

nhập vào số n tính n*n
xây dựng hàm UCLN - hiển thị UCLN và BCNN
                    - hiển thị ngoại lệ nhập a,b. (try/catch)
xây dựng hàm tính n! - hiển thị n! và ngoại lệ
kiểm tra số nguyên tố hiển thị các số nguyên tố < n và hiển thị ngoại lệ
hàm fibonacci - hiển thị n fibonacci
                - hiển thị n -> m fibonacci 
thêm:
        - tạo ngẫu nhiên dãy số
        - tính toán 
        - sắp xếp
        - chèn
        - xóa
        - v.v...

```